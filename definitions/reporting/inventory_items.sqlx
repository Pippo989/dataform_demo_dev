config {
  type: "table",
  database: "pipeline-muni",
  schema: "thelook_ecommerce",
  name: "inventory_items",
  description: "Clean inventory items table for reporting - identical to original",
  dependencies: ["stg_inventory_items"]
}

SELECT * FROM ${ref("stg_inventory_items")}