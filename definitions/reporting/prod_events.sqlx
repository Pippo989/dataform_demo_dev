config {
  type: "table",
  database: "pipeline-muni",
  schema: "thelook_ecommerce",
  name: "events",
  description: "Clean events table for reporting - identical to original",
  dependencies: ["stg_events"]
}

SELECT * FROM ${ref("stg_events")}