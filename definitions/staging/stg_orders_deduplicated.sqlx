config { type: "view" }

-- Step 1: Remove duplicate records by selecting the first occurrence of each order_id.
SELECT
  *
FROM (
  SELECT
    *,
    ROW_NUMBER() OVER(PARTITION BY order_id ORDER BY created_at ASC) as rn
  FROM
    ${ref("orders")}
)
WHERE
  rn = 1